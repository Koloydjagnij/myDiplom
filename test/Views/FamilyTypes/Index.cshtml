@model IEnumerable<test.FamilyType>

@{
    ViewData["Title"] = "Типы семьи";
}


<p>
    <div class="form-group">
        <a asp-action="Create"><input type="submit" value="Создать новую запись" class="btn btn-default" /></a>
    </div>

</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameFamilyType)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameFamilyType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFamilyType">Изменит</a> |
                <a asp-action="Details" asp-route-id="@item.IdFamilyType">Информация</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFamilyType">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
