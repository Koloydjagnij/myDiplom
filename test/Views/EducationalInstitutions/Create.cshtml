@model test.EducationalInstitution

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>EducationalInstitution</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NameEducationalInstitution" class="control-label"></label>
                <input asp-for="NameEducationalInstitution" class="form-control" />
                <span asp-validation-for="NameEducationalInstitution" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdTown" class="control-label"></label>
                <select asp-antiforgery="true" asp-for="IdTown" class ="form-control" asp-items="ViewBag.IdTown">
                <option selected="selected" disabled="disabled">Выберите город</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
