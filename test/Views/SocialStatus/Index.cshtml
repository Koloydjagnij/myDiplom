@model IEnumerable<test.SocialStatus>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameSocialStatus)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameSocialStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSocialStatus">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSocialStatus">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSocialStatus">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
