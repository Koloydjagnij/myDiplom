@model IEnumerable<test.SocialStatus>

@{
    ViewData["Title"] = "Соц. статусы";
}

<p>
    <div class="form-group">
        <a asp-action="Create"><input type="submit" value="Создать новую запись" class="btn btn-default" /></a>
    </div>

</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameSocialStatus)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameSocialStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSocialStatus">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.IdSocialStatus">Информация</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSocialStatus">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
