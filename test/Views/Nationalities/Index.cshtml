@model IEnumerable<test.Nationality>

@{
    ViewData["Title"] = "Национальности";
}

<p>
    <div class="form-group">
        <a asp-action="Create"><input type="submit" value="Создать новую запись" class="btn btn-default" /></a>
    </div>

</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameNationality)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameNationality)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdNationality">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.IdNationality">Информация</a> |
                <a asp-action="Delete" asp-route-id="@item.IdNationality">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
