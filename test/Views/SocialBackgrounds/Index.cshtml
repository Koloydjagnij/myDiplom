@model IEnumerable<test.SocialBackground>

@{
    ViewData["Title"] = "Социальные происхождения";
}

<p>
    <div class="form-group">
        <a asp-action="Create"><input type="submit" value="Создать новую запись" class="btn btn-default" /></a>
    </div>

</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameSocialBackground)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameSocialBackground)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSocialBackground">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.IdSocialBackground">Информация</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSocialBackground">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
